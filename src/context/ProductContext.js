import { createContext } from "react";

export default createContext({
  inputTitle: "",
  setInputTitle: () => {},
  inputImageSource: "",
  setInputImageSource: () => {},
  inputPrice: "",
  setInputPrice: () => {},
});
